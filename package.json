{
  "name": "linkify",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @linkify/api build",
    "start": "pnpm --filter @linkify/api start",
    "dev": "pnpm --filter @linkify/api dev",
    "test": "pnpm --filter @linkify/api test",
    "test:watch": "pnpm --filter @linkify/api test:watch",
    "typecheck": "pnpm --filter @linkify/api typecheck",
    "db:generate": "pnpm --filter @linkify/api db:generate",
    "db:push": "pnpm --filter @linkify/api db:push",
    "db:studio": "pnpm --filter @linkify/api db:studio",
    "db:seed": "pnpm --filter @linkify/api db:seed",
    "prepare": "husky"
  },
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^15.5.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "esbuild"
    ]
  }
}
